spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ownday?serverTimezone=UTC
    username : ssafy
    password : ssafy

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.ownday.model.dto